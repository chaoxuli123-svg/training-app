# 🚀 快速部署到 Vercel

## ✅ 项目已经打包完成！

构建文件位于 `dist` 目录，大小约 614 KB。

## 📋 部署步骤（3种方法任选）

---

## 方法一：Vercel CLI 部署（最快）

### 1. 登录 Vercel

在终端运行：

```bash
vercel login
```

选择登录方式：
- **Email** - 通过邮箱登录
- **GitHub** - 通过GitHub登录（推荐）
- **GitLab** - 通过GitLab登录
- **Bitbucket** - 通过Bitbucket登录

### 2. 部署项目

登录成功后，在项目目录运行：

```bash
cd /Users/lichaoxu/test-project/training-app
vercel
```

首次部署会询问几个问题，直接按回车使用默认值即可：
- Set up and deploy? **Y**
- Which scope? **选择你的账户**
- Link to existing project? **N**
- What's your project's name? **training-app**
- In which directory is your code located? **./**
- Want to override the settings? **N**

几秒钟后会给你一个预览URL！

### 3. 部署到生产环境（可选）

预览成功后，部署到生产：

```bash
vercel --prod
```

---

## 方法二：网页直接上传（无需Git）

### 1. 访问 Vercel

打开浏览器访问：https://vercel.com/

### 2. 登录/注册

使用GitHub、GitLab或Email登录。

### 3. 手动部署

1. 点击右上角 "Add New..." → "Project"
2. 选择 "Deploy with Vercel CLI" 或直接拖拽 `dist` 文件夹
3. 等待上传完成

**注意**：如果直接拖拽dist文件夹，这是静态文件部署，无法自动重新构建。

---

## 方法三：通过 GitHub（推荐用于长期维护）

### 1. 初始化 Git 仓库

```bash
cd /Users/lichaoxu/test-project/training-app
git init
git add .
git commit -m "Initial commit: Personal Training Assistant"
```

### 2. 推送到 GitHub

在 GitHub 创建新仓库：https://github.com/new

然后推送代码：

```bash
git remote add origin https://github.com/你的用户名/仓库名.git
git branch -M main
git push -u origin main
```

### 3. 在 Vercel 导入 GitHub 仓库

1. 访问 https://vercel.com/new
2. 点击 "Import Git Repository"
3. 选择你的 GitHub 仓库
4. Vercel 会自动检测到 Vite 项目
5. 确认配置：
   - Framework Preset: **Vite**
   - Build Command: **npm run build**
   - Output Directory: **dist**
6. 点击 "Deploy"

### 优势

- ✅ 每次 git push 自动重新部署
- ✅ 自动预览每个分支
- ✅ 回滚到任何历史版本

---

## 🎯 推荐操作流程

**如果你想快速体验**：
→ 使用方法一（Vercel CLI）

**如果你想长期使用和维护**：
→ 使用方法三（GitHub + Vercel）

---

## 📱 部署后的应用信息

部署成功后，你会得到：

- **预览URL**: `https://training-app-xxx.vercel.app`
- **生产URL**: `https://training-app.vercel.app`（使用 --prod 后）

### 应用特性

✅ 完全免费（Vercel个人版）  
✅ 自动 HTTPS  
✅ 全球CDN加速  
✅ 无限次部署  
✅ 实时性能监控  
✅ 自动优化  

### 功能验证清单

部署后请验证：
- [ ] 页面正常加载
- [ ] 7个导航标签可以切换
- [ ] 可以添加跑步记录
- [ ] 可以添加力量训练记录
- [ ] 可以添加身体数据
- [ ] 图表正常显示
- [ ] 数据保存在本地（刷新页面数据还在）
- [ ] 配速计算正确

---

## ⚡ 立即开始

**第一步：登录 Vercel**

```bash
vercel login
```

**第二步：部署**

```bash
vercel
```

**完成！** 🎉

---

## 💡 提示

- 首次部署可能需要1-2分钟
- 后续更新部署只需10-30秒
- 所有用户数据保存在浏览器本地，不依赖服务器
- 可以分享URL给朋友使用（每个人的数据是独立的）

---

## 🆘 遇到问题？

### 问题1：vercel: command not found
解决：重新打开终端，或运行 `source ~/.zshrc`

### 问题2：登录失败
解决：确保网络正常，尝试使用GitHub登录

### 问题3：部署失败
解决：检查 `npm run build` 是否成功

### 问题4：页面打开是空白
解决：查看浏览器控制台错误信息，通常是构建配置问题

---

**项目已准备就绪，随时可以部署！** 🚀
