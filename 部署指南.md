# 部署到 Vercel 指南

## 方法一：通过 Vercel CLI（推荐）

### 1. 安装 Vercel CLI

```bash
npm install -g vercel
```

### 2. 登录 Vercel

```bash
vercel login
```

### 3. 部署项目

在项目目录中运行：

```bash
cd /Users/lichaoxu/test-project/training-app
vercel
```

首次部署时会询问：
- **Set up and deploy?** → Yes
- **Which scope?** → 选择你的账户
- **Link to existing project?** → No
- **Project name?** → training-app（或自定义名称）
- **In which directory is your code located?** → ./
- **Want to modify settings?** → No

部署完成后会给你一个预览URL。

### 4. 部署到生产环境

```bash
vercel --prod
```

## 方法二：通过 Vercel 网页界面

### 1. 初始化 Git 仓库（如果还没有）

```bash
cd /Users/lichaoxu/test-project/training-app
git init
git add .
git commit -m "Initial commit: Personal Training Assistant"
```

### 2. 推送到 GitHub

在 GitHub 创建新仓库，然后：

```bash
git remote add origin https://github.com/你的用户名/training-app.git
git branch -M main
git push -u origin main
```

### 3. 在 Vercel 导入项目

1. 访问 https://vercel.com/
2. 点击 "Add New..." → "Project"
3. 选择 "Import Git Repository"
4. 选择你刚推送的 GitHub 仓库
5. 配置项目：
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`
6. 点击 "Deploy"

## 方法三：直接上传（最简单）

### 使用 Vercel CLI 直接部署当前目录

```bash
cd /Users/lichaoxu/test-project/training-app
vercel --prod
```

按照提示操作即可。

## 部署配置说明

项目已包含 `vercel.json` 配置文件：

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "vite"
}
```

## 验证部署

部署成功后，你会获得一个URL，例如：
- 预览: `https://training-app-xxx.vercel.app`
- 生产: `https://training-app.vercel.app`

访问URL确认：
- ✅ 页面正常加载
- ✅ 底部导航可以切换
- ✅ 可以添加训练记录
- ✅ 数据保存在浏览器本地
- ✅ 图表正常显示

## 环境要求

- Node.js >= 18
- npm >= 9

## 常见问题

### Q: 部署后页面空白？
A: 检查浏览器控制台，确保没有路径错误。Vite默认使用相对路径，应该没问题。

### Q: 数据会丢失吗？
A: 不会。数据保存在用户浏览器的 LocalStorage 中，与部署无关。

### Q: 可以自定义域名吗？
A: 可以。在 Vercel 项目设置中添加自定义域名。

### Q: 免费吗？
A: Vercel 个人项目免费，有充足的配额。

## 更新部署

修改代码后重新部署：

```bash
# 方式1: 如果使用Git
git add .
git commit -m "更新描述"
git push
# Vercel会自动重新部署

# 方式2: 直接部署
vercel --prod
```

## 性能优化建议

当前构建大小：614.67 kB

如果需要优化：
1. 使用动态导入拆分代码
2. 按需加载Recharts组件
3. 优化图片资源

但对于这个应用来说，当前大小是可接受的。

## 监控和分析

Vercel自动提供：
- ✅ 性能监控
- ✅ 错误追踪
- ✅ 访问统计
- ✅ 构建日志

在Vercel控制台可以查看这些信息。

---

**准备就绪！现在可以部署了！** 🚀
